version: "3.1"

rules:
  - rule: Handle everything through our custom action script
    steps:
      - intent: ask_faq
      - action: action_handle_query

  - rule: Handle weather requests
    steps:
      - intent: ask_weather
      - action: action_handle_query

  - rule: Handle general GPT questions
    steps:
      - intent: ask_gpt
      - action: action_handle_query
